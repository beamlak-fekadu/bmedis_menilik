# Formulas and Weights Evidence

| Formula | File / Function | Definition | Criteria / Weights | Output / Bands | Active in Menelik? | Maturity Caution |
| --- | --- | --- | --- | --- | --- | --- |
| RPN / FMEA risk | src/utils/analytics/formulas.ts computeRPN; supabase/migrations/00036_fmea_risk_engine.sql fn_compute_fmea_risk_for_asset | RPN = Severity × Occurrence × Detectability | Severity, occurrence, detectability each 1-10; multiplicative, not weighted. | 1-1000; low <80, medium >=80, high >=200, critical >=500. | Yes; equipment_risk_scores rows initialized. | Occurrence/detectability depend on limited imported work/PM/calibration history. |
| Availability | src/utils/analytics/formulas.ts computeAvailability; supabase/migrations/00011_views_and_functions.sql fn_compute_availability | Availability = MTBF / (MTBF + MTTR) | No weights. | 0-1 ratio. | Yes as reliability metric field. | Requires MTBF and MTTR; current maintenance_events/downtime history is limited/empty. |
| MTBF | src/utils/analytics/formulas.ts computeMTBF; SQL fn_compute_mtbf | MTBF = total operational hours / failure count | No weights. | Hours or null when no failures. | Yes. | No recorded failures can produce null. |
| MTTR | src/utils/analytics/formulas.ts computeMTTR; SQL fn_compute_mttr | MTTR = total maintenance/repair hours / repair count | No weights. | Hours or null when no repair duration evidence. | Yes. | Requires completed maintenance_events with repair_duration_hours. |
| PM compliance | src/utils/analytics/formulas.ts computePMC; SQL fn_compute_pmc; score-registry pm_compliance | PMC = completed scheduled PM tasks / total scheduled PM tasks × 100 | No weights. | 0-100%. | Workflow evidence imported; stored pm_compliance_metrics count is 0 in validation. | Do not claim hospital-wide PM compliance from 14 imported PM/PV rows. |
| Min-max normalization | src/utils/analytics/normalization.ts | NormScore = (value - min) / (max - min); inverse = 1 - normalized | Used before weighted composites. | 0-1; no variance defaults to 0.5 in callers. | Yes through RPI and composite logic. | Scores reflect available baseline variance. |
| Replacement Priority Index / RPI | src/utils/analytics/replacement-index.ts; SQL compute_replacement_priority_scores_all | Weighted sum of normalized age, failure, inverse availability, maintenance burden, spare-part, risk, and cost scores. | Age 15%, failures 15%, availability 20%, maintenance burden 15%, spare parts 10%, risk 15%, cost 10%. | 0-1; monitor <0.55, review >=0.55, strong >=0.70. | Yes; replacement_priority_scores initialized. | Inputs such as cost/spare/maintenance burden are limited by current workbook. |
| Equipment health score | src/services/decision-support.service.ts computeFromOperationalData; SQL refresh_decision_support_snapshots | Health = availability 35 + PM compliance 25 + inverted RPN control 25 + condition/status 15. | Availability 35, PM compliance 25, risk control 25, condition/status 15. | Approx. 1-100 score. | Yes; snapshot rows queried. | Defaults are used when supporting rows are missing. |
| Clinical readiness | score-registry department_clinical_readiness; SQL refresh_decision_support_snapshots | Readiness = essential functional assets / total essential assets × 100. | Rule-based ratio, not weighted. | 0-100%. | Yes; readiness rows queried. | Snapshot freshness must be checked. |
| Critical action score | src/utils/analytics/critical-action-bands.ts; command-center-data.ts buildCriticalActions | Critical action score = category base weight + item priority score. | Corrective 100, needs_request 90, calibration 85, PM 75, stock 70, risk_watch 65, installation 60, replacement 55, procurement 45, training 35. | critical >=180, high >=150, medium >=100, else low. | Yes in Command Center. | Live ranking depends on current module data; many modules are empty in current Menelik import. |
| Stock blocker priority | score-registry stock_blocker_priority; command-center-data.ts fetchStockBlockers | Priority = 100 if linked to active work, else 90 stockout, else 60 + reorder deficit ratio × 30. | Open work linkage, current_stock, reorder_level. | Live priority score. | Implemented. | No spare parts imported from current workbook. |
| Procurement delay priority | src/utils/decision-support/procurement-delay.ts scoreProcurementDelay | If delayed: base 60 + min(120, days past due × 2) + priority boost. If no expected date: 25 + min(45, age days × 0.5) + priority boost. | Priority boost: critical +40, high +20, medium 0, low -10. Terminal delivered/canceled score 0. | critical >=150, high >=100, medium >=60, low otherwise. | Implemented. | No Menelik procurement rows verified from current workbook. |
| PM triage priority | score-registry pm_triage_priority; command-center-data.ts fetchPMTriage | Priority = 50 + min(50, overdue days × 0.3). | Scheduled date and overdue days. | Used as item score in critical action ranking. | Implemented. | Imported PM/PV schedules are completed, so overdue PM may be 0. |
| Calibration risk priority | score-registry calibration_risk_priority; command-center-data.ts fetchCalibrationTriage | Priority = 50 + min(50, overdue days × 0.5). | Next due date and overdue days. | Used as item score in critical action ranking. | Implemented. | No Menelik row-level calibration records imported. |
| Decision support snapshots | supabase/migrations/00023... refresh_decision_support_snapshots; src/actions/decision-support.actions.ts | Refreshes health, readiness, workload, and triage snapshots from source tables. | Uses formulas above and latest source rows. | Snapshot tables and triage queue. | analyticsRefresh completed in import-report.json. | Refresh completion is not the same as mature longitudinal data. |

## Thesis Table Accuracy Notes

- If thesis Table 1 says RPN = S × O × D with 1-10 factors and bands low/medium/high/critical, it matches current code.
- If thesis Table 2 says RPI uses age, failure, availability, maintenance burden, spare support, risk, and cost with weights 15/15/20/15/10/15/10, it matches current code.
- Any table claiming mature MTBF, MTTR, availability, PM compliance, or calibration compliance from the Menelik import must be revised because the current imported history is limited.
